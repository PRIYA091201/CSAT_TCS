{
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter @csat/kiosk run build",
  "outputDirectory": "apps/kiosk/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}